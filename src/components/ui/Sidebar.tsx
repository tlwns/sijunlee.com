import { Flex } from '@chakra-ui/react';

const Sidebar = () => {
  return (
    <Flex dir='column'>
      <Flex>{/* Headers */}</Flex>
      <Flex>{/* About me block */}</Flex>
    </Flex>
  );
};

export default Sidebar;
